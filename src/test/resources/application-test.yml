spring:
  datasource:
    url: jdbc:mysql://${MYSQL_HOST:localhost}:3306/ting_test?serverTimezone=UTC
    username: ${MYSQL_USER:root}
    password: ${MYSQL_PASSWORD:123456}
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: create
